services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: campus_asset_inspection
      POSTGRES_USER: campus
      POSTGRES_PASSWORD: campus123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
